# Built-in Extensions

This directory is the default built-in plugin discovery root used by
`ExtensionManager`.

Built-in plugins should be added as child directories containing a
`plugin.yaml` manifest. The directory is intentionally allowed to be empty.
