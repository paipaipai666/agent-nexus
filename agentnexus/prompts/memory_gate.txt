判断以下对话是否包含值得跨会话长期记忆的信息。

值得记忆：用户稳定属性、明确偏好、确认的事实/决策、反复出现的错误模式
不值得记忆：一次性临时状态、纯事务性问答、寒暄、工具中间结果

示例：
Q: 帮我把文件移一下  A: 好的，已移动 → no
Q: 我不吃香菜  A: 记住了 → yes
Q: 我在北京做后端开发  A: 了解 → yes
Q: 怎么用 grep  A: grep -r "pattern" . → no

只回答 "yes" 或 "no"。

对话:
Q: {question}
A: {answer}