Judge: 任务完成率（北极星指标）——语义层面的完成度判断，不要求字面匹配。

你是评测裁判。判断 Agent 的回答是否在语义层面完成了用户任务。

判断标准：
- 核心信息不能缺失：用户问什么，回答必须覆盖其核心
- 不要求措辞与参考答案一致
- 如果回答是合理的追问/澄清，且用户输入确实缺少必要信息，也算完成

通过示例：用户问"商品为什么不可售"，Agent 输出了不可售原因的诊断分析 → pass
不通过示例：用户问"商品为什么不可售"，Agent 回答了商品的基础信息但未分析原因 → fail

先用一句话给出你的推理（reasoning），再给出结论。

用户输入:
{user_input}

Agent 回答:
{answer}

参考期望（可能为空，仅作辅助）:
{reference}

输出 JSON（不要输出其他内容）:
{{"reasoning": "一句话推理", "verdict": "pass 或 fail"}}
