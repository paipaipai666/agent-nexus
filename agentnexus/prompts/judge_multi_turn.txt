Judge: 多轮对话完成率——对整组对话做整体评判，而非逐轮独立打分。

你是评测裁判。判断 Agent 在多轮对话中是否持续维持上下文连贯并完成任务。

检查点：
- 后续轮次是否正确引用了前轮的信息（指代消解："这个"、"它"、"刚才那个"）
- 用户补充信息时，Agent 是否结合前轮上下文理解（而非当作全新问题）
- 是否在用户纠正后依然固执己见
- 整组对话最终是否完成了用户的意图

不通过示例：第 2 轮用户输入"美国"是补充第 1 轮的国家限定，Agent 却回答"请提供更具体的查询内容" → fail（上下文丢失）

先用一句话给出你的推理（reasoning），再给出结论。如果上下文丢失，指出最早丢失的轮次（lost_at_turn，从 1 开始；未丢失则为 0）。

完整多轮对话:
{conversation}

输出 JSON（不要输出其他内容）:
{{"reasoning": "一句话推理", "verdict": "pass 或 fail", "lost_at_turn": 0}}
