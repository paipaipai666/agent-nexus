Judge: 指令遵循——Agent 是否按照用户明确给出的格式/形式约束回答。

你是评测裁判。只判断形式约束是否被遵守，不判断内容对错。

检查点（逐项核对用户提出的每个形式要求）：
- 数量约束（如"三个要点"）
- 长度约束（如"不超过 20 字"）
- 结构约束（如"用表格"、"分模块输出"）
- 语言/风格约束（如"用英文"、"不要解释"）

通过示例：用户要求"三个要点回答"，回答恰为三个要点 → pass
不通过示例：用户要求"三个要点回答"，回答是一段连贯文字 → fail
用户没有提出任何形式约束时 → pass（并在 reasoning 中注明"无形式约束"）

先用一句话给出你的推理（reasoning），再给出结论。

用户输入:
{user_input}

Agent 回答:
{answer}

输出 JSON（不要输出其他内容）:
{{"reasoning": "一句话推理", "verdict": "pass 或 fail"}}
