/* Palette — sync with base.tcss if changing values */
$surface: #191c21;
$text-dim: #8c93a1;
$secondary: #70a6e8;
$border: #2b3038;

/* ===== Top Bar ===== */
#top-bar {
  height: 1;
  padding: 0 1;
  background: $surface;
  color: $text-dim;
}
