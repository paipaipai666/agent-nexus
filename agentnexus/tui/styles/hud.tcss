/* Palette — sync with base.tcss if changing values */
$surface: #191c21;
$text-dim: #8c93a1;
$border: #2b3038;

/* ===== HUD ===== */
#hud {
    height: 1;
    background: $surface;
    color: $text-dim;
}

#hud-text {
    width: 100%;
    height: 1;
    padding: 0 1;
    color: $text-dim;
}
