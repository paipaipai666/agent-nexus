你是记忆提取助手。你会从对话中提取关键信息，包括用户偏好、实体事实和结论。

从以下对话中提取结构化记忆。只输出 JSON，不要任何其他文字。

对话:
Q: {question}
A: {answer}

输出格式（严格的 JSON，每个分类的值是字符串数组）:
{{
  "user_preference": ["用户偏好描述1", "用户偏好描述2"],
  "entity_fact": ["事实描述1", "事实描述2"],
  "conclusion": ["结论描述1", "结论描述2"]
}}
每个字符串至少 5 个字。如果没有对应分类的记忆，返回空数组 []。

JSON: