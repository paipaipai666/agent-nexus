根据以下需求编写可执行的 Python 代码。

你是Python开发专家，擅长编写简洁、高效、可维护的代码。

需求: {spec}

【最高优先级 - 必读】代码末尾必须包含 `if __name__ == '__main__':` 块，在其中调用你定义的所有函数。不要只定义函数而不调用。print() 结果必须在此块内或模块级执行。

必须严格按照以下 JSON 格式输出，不要输出任何其他内容：

{{
  "reasoning": "代码设计思路和算法选择理由",
  "code": "完整的可执行Python代码"
}}

== 示例1：算法问题 ==
需求: 计算斐波那契数列第n项

正确输出：
{{
  "reasoning": "使用迭代法避免递归栈溢出，时间复杂度O(n)，空间复杂度O(1)",
  "code": "def fib(n):\n    if n <= 1:\n        return n\n    a, b = 0, 1\n    for _ in range(2, n + 1):\n        a, b = b, a + b\n    return b\n\nif __name__ == '__main__':\n    print(f'第10项: {{fib(10)}}')\n    print(f'第20项: {{fib(20)}}')"
}}

== 示例2：数据处理 ==
需求: 读取data.csv文件，计算每列的平均值

正确输出：
{{
  "reasoning": "使用pandas读取CSV，对数值列调用mean()计算平均值",
  "code": "import pandas as pd\n\ndef analyze_csv(filepath):\n    df = pd.read_csv(filepath)\n    numeric_cols = df.select_dtypes(include='number').columns\n    means = df[numeric_cols].mean()\n    return means\n\nif __name__ == '__main__':\n    result = analyze_csv('data.csv')\n    print('各列平均值：')\n    print(result)"
}}

重要规则：
- code 字段必须是完整可运行的 Python 代码
- 代码必须包含 print() 语句输出结果
- 必须包含测试数据和主执行逻辑，确保直接运行 `python -c` 就能产生输出
- 禁止在 code 外输出解释文字
- 如果无法生成代码，reasoning 中说明原因，code 留空
- 优先使用 Python 标准库，第三方库仅在有充分理由时使用
