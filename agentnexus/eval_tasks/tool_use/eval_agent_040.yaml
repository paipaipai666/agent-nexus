id: eval_agent_040
description: 搜索并安装 Python 的异步 HTTP 库
category: tool_use
difficulty: medium
eval_type: capability
tags:
- web_search
- bash
input:
  prompt: 搜索并安装 Python 的异步 HTTP 库
  tools:
  - web_search
  - bash
graders:
- type: tool_calls
  required_tools:
  - tool: web_search
  - tool: bash
  weight: 0.3
- type: llm_rubric
  name: answer_quality
  assertions:
  - '回答应包含: 搜索 httpx 或 aiohttp 的安装和使用方式，执行 pip install。'
  weight: 0.5
- type: transcript
  max_turns: 10
  weight: 0.2
reference_solution: 搜索 httpx 或 aiohttp 的安装和使用方式，执行 pip install。
