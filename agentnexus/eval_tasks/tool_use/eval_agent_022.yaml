id: eval_agent_022
description: 从 URL 下载文件并保存到本地
category: tool_use
difficulty: medium
eval_type: capability
tags:
- bash
input:
  prompt: 从 URL 下载文件并保存到本地
  tools:
  - bash
graders:
- type: tool_calls
  required_tools:
  - tool: bash
  weight: 0.3
- type: llm_rubric
  name: answer_quality
  assertions:
  - '回答应包含: 使用 curl 或 wget 下载文件，保存到指定路径，验证下载完整性。'
  weight: 0.5
- type: transcript
  max_turns: 10
  weight: 0.2
reference_solution: 使用 curl 或 wget 下载文件，保存到指定路径，验证下载完整性。
