id: eval_agent_047
description: 生成有效的 requirements.txt（仅生产依赖）
category: coding
difficulty: medium
eval_type: capability
tags:
- bash
- write
input:
  prompt: 生成有效的 requirements.txt（仅生产依赖）
  tools:
  - bash
  - write
graders:
- type: tool_calls
  required_tools:
  - tool: bash
  - tool: write
  weight: 0.3
- type: llm_rubric
  name: answer_quality
  assertions:
  - '回答应包含: 使用 pip list --format=freeze 过滤出非 dev 依赖，写入 requirements.txt。'
  weight: 0.5
- type: transcript
  max_turns: 10
  weight: 0.2
reference_solution: 使用 pip list --format=freeze 过滤出非 dev 依赖，写入 requirements.txt。
