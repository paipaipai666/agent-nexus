"""Desktop application automation via OS-level accessibility APIs.

Supports Windows (UIA via pywinauto), Linux (AT-SPI2), and macOS (AX via pyobjc).
LLM sees the same YAML accessibility tree format as browser mode.
"""

from __future__ import annotations
