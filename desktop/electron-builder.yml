appId: com.agentnexus.desktop
productName: AgentNexus
directories:
  output: release

files:
  - dist/**/*
  - dist-electron/**/*

win:
  target:
    - target: nsis
      arch:
        - x64
  extraResources:
    - from: "backend/agentnexus.exe"
      to: "backend/agentnexus.exe"
    - from: "backend/_internal"
      to: "backend/_internal"

mac:
  target:
    - target: dmg
      arch:
        - x64
        - arm64
  extraResources:
    - from: "backend/agentnexus"
      to: "backend/agentnexus"
    - from: "backend/_internal"
      to: "backend/_internal"

linux:
  target:
    - AppImage
    - deb
  category: Utility
  extraResources:
    - from: "backend/agentnexus"
      to: "backend/agentnexus"
    - from: "backend/_internal"
      to: "backend/_internal"

nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
